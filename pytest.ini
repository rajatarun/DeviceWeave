[pytest]
# Makes `src/` importable as top-level modules (e.g. `import app`,
# `import decision_engine`) without a conftest.py sys.path hack per test file.
pythonpath = src
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
