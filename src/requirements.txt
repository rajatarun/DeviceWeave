# Runtime dependencies for DeviceWeave Lambda functions.
#
# aiohttp          — async HTTP client used by all three provider adapters
#                    (Kasa cloud REST, SwitchBot v1.1, Govee developer API)
# neo4j            — Bolt protocol driver for Memgraph graph database
#                    (behavior engine, scene graph storage)
# mcp-observatory  — telemetry instrumentation for Bedrock/Claude API calls
# boto3            — AWS SDK (used by mcp-observatory for DynamoDB metrics)
# google-genai      — Google Gemini SDK for Live API conversations with tool support

aiohttp>=3.9
neo4j>=5.0
ring-doorbell>=0.9
python-myq>=3.1.13
wyze-sdk>=2.0.0
mcp-observatory==0.2.0
boto3>=1.26.0
google-genai>=0.3.0
